# Pulse frontend

React + TypeScript + Vite frontend for Pulse. `/` is the product landing page. The live dashboard is at `/dashboard`.

```bash
cp .env.example .env
npm install
npm run dev
```

Then open http://localhost:5173. The API should be running at the URL in `VITE_API_BASE_URL` (default `http://localhost:8080`).
